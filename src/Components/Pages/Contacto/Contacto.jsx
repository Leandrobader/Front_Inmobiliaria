import React from 'react';
import { useNavigate } from "react-router-dom";

const Contacto = () => {
    return (
        <div>
            Desde la página Contacto
        </div>
    );
};

export default Contacto;